



import 'package:flutter/material.dart';

class IconCompany extends StatelessWidget {
  const IconCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        
      ],
    );
  }
}